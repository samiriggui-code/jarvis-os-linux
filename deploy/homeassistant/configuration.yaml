# Home Assistant — NUC (cerveau domotique). Déployé via deploy/homeassistant/compose.nuc.yaml
#
# Source YAML versionnée ici ; runtime /opt/jarvis/homeassistant/config sur NUC.
# Pi salon = I/O seulement (cam, ear, enceinte) — pas de second HA.

default_config:

frontend:
  themes: !include_dir_merge_named themes

automation: !include automations.yaml
script: !include scripts.yaml
scene: !include scenes.yaml

# Chaque fichier de `packages/` est un bloc de configuration autonome. Ça évite
# le `configuration.yaml` de 800 lignes où plus personne ne retrouve rien.
homeassistant:
  packages: !include_dir_named packages

# Tableau de bord en mode YAML, à côté de celui généré automatiquement.
# `mode: yaml` interdit l'édition par l'interface : le fichier gagne toujours.
lovelace:
  mode: storage
  dashboards:
    jarvis-maison:
      mode: yaml
      title: Maison
      icon: mdi:home-assistant
      show_in_sidebar: true
      filename: dashboards/jarvis.yaml
