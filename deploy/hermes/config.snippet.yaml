# Toolsets api_server — plafond Hermes pour le Core.
# FULL par défaut (tout sauf homeassistant) — voir hermes_toolsets.py
# JARVIS_HERMES_MINIMAL=1 dans core.env → base (web, skills, memory…)
# JARVIS_HERMES_CHAT_ONLY=1 → web + skills (latence chat)
platform_toolsets:
  api_server:
    - web
    - skills
    - memory
    - todo
    - session_search
    - vision
    - browser
    - file
    - terminal
    - code_execution
    - cronjob
    - delegation
    - a2a
    - computer_use
    - clarify
    - x_search
    - image_gen
    - bfl
    - video
    - video_gen
    - spotify
    - tts
    - stt
    - context_engine
    - discord
    - discord_admin
    - yuanbao
    # homeassistant — INTERDIT (domotique = couche JARVIS → HA NUC)
