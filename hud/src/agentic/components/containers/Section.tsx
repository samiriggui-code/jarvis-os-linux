/**
 * Section — alias de `layout/SectionFrame.tsx` (déjà le renderer réel utilisé
 * par `Workspace`) ; pas de second renderer.
 */
export { SectionFrame as Section } from '../../layout/SectionFrame';
export type { SectionFrameProps as SectionProps } from '../../layout/SectionFrame';
